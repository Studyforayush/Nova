import { _Input } from '../model/pipeline';
import { Paginated } from '../clustering';
import Logger from '../logging';
import { HttpApiClient, ApiReqParams } from './client';
export default class ClusteringApiClient {
    private client;
    private logger?;
    rootPath: string;
    constructor(client: HttpApiClient, logger?: Logger);
    get(path: string, params?: ApiReqParams): Promise<any>;
    getPaginated<T>(path: string, field: string, mapFn: (raw: any) => T, limit?: number, params?: ApiReqParams): Paginated<T>;
    postItems(path: string, items: _Input<string>[], params?: ApiReqParams & {
        forceNewClusters?: boolean;
        forceClusterId?: number;
    }): Promise<any>;
    post(path: string, body: any, params?: ApiReqParams): Promise<any>;
    delete(path: string, params?: ApiReqParams): Promise<any>;
}
